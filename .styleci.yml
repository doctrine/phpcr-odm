preset: symfony

enabled:
  - alpha_ordered_imports
  - short_array_syntax

disabled:
  - single_line_throw
  # we have documentation generated from phpdoc where we use "Something:" as first line
  - phpdoc_summary
